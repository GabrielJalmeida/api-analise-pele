{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python criar_banco.py && python popular_catalogo.py --aplicar && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/status",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
